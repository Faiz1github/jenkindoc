pipeline{
      agent any
      stages{
         stage ('build') {   
             steps{
                    sh 'git --version'
                    sh 'du -h'
                   }
               }
            }
         } 
      
