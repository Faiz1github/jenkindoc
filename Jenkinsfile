pipeline{
      agent{ docker {image 'ubuntu'}}
      stages{
         stage ('build') {   
             steps{
                    sh 'git --version'
                   }
               }
            }
         } 
      
